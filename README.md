# DRCR2

One token. It is an **op**.

```ts
type Token = { type: 'op'; value: '+' | '-' | '*' | '/' };
```

`+`, `-`, `*`, `/` — or the words plus, minus, times, divided.

```bash
npm install
npm run typecheck
npm test
npm run dev
```
